# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono bold 9
# font pango:Source Code Pro 10
# font pango: YaHei Consolas Hybrid 9
# font pango: Microsoft YaHei 9
font pango:DejaVu Sans YuanTi bold 9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Auto switch workspaces back and forth
workspace_auto_back_and_forth yes

force_focus_wrapping yes
workspace_layout tabbed

# workspace "0:web" output CRT1
# workspace "1:code" output CRT1
# workspace "2:db" output CRT1

# Test area
# bindsym $mod+n workspace number 3; exec dbeaver; workspace number 1

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
# bindsym $mod+Shift+q kill
bindsym $mod+q kill
bindsym Mod1+F4 kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -i -b -fn "Source Code Pro-12" -p $
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
bindsym Mod1+Tab focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h
# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# Workspace renaming
bindsym $mod+Mod1+r exec i3-input -F 'exec --no-startup-id workspace.rb %s' -P 'New name: '

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle
# focus the parent container
bindsym $mod+a focus parent
# focus the child container
bindsym $mod+c focus child
# focus outputs
bindsym $mod+Control+Left focus output left
bindsym $mod+Control+Right focus output right

# switch to workspace
bindsym $mod+0 workspace 0:web
bindsym $mod+1 workspace 1:code
bindsym $mod+2 workspace 2:db
bindsym $mod+3 workspace 3:vcs
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace 9:console
bindsym $mod+Mod1+Left workspace prev_on_output
bindsym $mod+Mod1+Right workspace next_on_output
bindsym Control+Mod1+j workspace prev_on_output
bindsym Control+Mod1+semicolon workspace next_on_output

# move focused container to workspace
bindsym $mod+Shift+0 move container to workspace 0:web
bindsym $mod+Shift+1 move container to workspace 1:code
bindsym $mod+Shift+2 move container to workspace 2:db
bindsym $mod+Shift+3 move container to workspace 3:vcs
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace 9:console

# Movements
# # move containers
bindsym $mod+Control+Shift+Left move container to workspace prev
bindsym $mod+Control+Shift+Right move container to workspace next
bindsym $mod+Mod1+Shift+Left move container to output left
bindsym $mod+Mod1+Shift+Right move container to output right
# # move workspace to outputs
bindsym $mod+Control+Mod1+Shift+Left move workspace to output left
bindsym $mod+Control+Mod1+Shift+Right move workspace to output right

# Scratchpad
# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad
# Show the first scratchpad window
bindsym $mod+minus scratchpad show
# Show the sup-mail scratchpad window, if any.
# bindsym mod4+s [title="^Sup ::"] scratchpad show

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+e exec "zenity --question --text='注销当前用户？' && i3-msg exit"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 1 ppt
        bindsym k resize grow height 10 px or 1 ppt
        bindsym l resize shrink height 10 px or 1 ppt
        bindsym semicolon resize grow width 10 px or 1 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 1 ppt
        bindsym Down resize grow height 10 px or 1 ppt
        bindsym Up resize shrink height 10 px or 1 ppt
        bindsym Right resize grow width 10 px or 1 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+t border normal
bindsym $mod+y border pixel 1
bindsym $mod+u border pixel 0

new_window 1pixel
new_float 1pixel
# hide_edge_borders none

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# i3bar on LVDS1 or HDMI1
bar {
    # output CRT1
    # font pango:DejaVu Sans YuanTi 10
    # font pango:DS-Digital 14
    font pango:Zekton 12
    position top
    status_command i3status
    # mode hide
    # modifier Mod1
    # colors {
        # background #073642
        # focused_workspace #FFA500 #A52A2A #ffffff
        # active_workspace #FFA500 #CB4B16 #ffffff
        # inactive_workspace #FFA500 #B58900 #ffffff
    # }
    # colors{
        # background #232323
        # statusline #DCDCDC
    # }
}

# # Brown and golden theme
# # class                 border  backgr. text    indicator
# client.focused          #FFA500 #A52A2A #ffffff #2e9ef4
# client.unfocused        #FFA500 #B58900 #ffffff #292d2e
# client.focused_inactive #FFA500 #CB4B16 #ffffff #484e50
# client.urgent           #2f343a #900000 #ffffff #900000

# class                 border  backgr. text    indicator
client.focused          #FF0000 #A52A2A #ffffff #2e9ef4
client.unfocused        #FF7500 #B35C44 #cccccc #292d2e
client.focused_inactive #F20C00 #C93756 #ffffff #484e50
client.urgent           #2f343a #900000 #ffffff #900000

# # lkraav's solarized theme
# # class                 border  backgr. text    indicator
# client.focused          #859900 #859900 #fdf6e3 #2e9ef4
# client.focused_inactive #268bd2 #268bd2 #fdf6e3 #484e50
# client.unfocused        #333333 #93a1a1 #fdf6e3 #292d2e

# # hndr's theme
# client.focused          #3F8AC2 #096BAA #00BAA7 #00DA8E
# client.focused_inactive #333333 #5F676A #ffffff #484e50
# client.unfocused        #333333 #424242 #888888 #292d2e
# client.urgent           #C10004 #900000 #ffffff #900000

# exec yunio
# exec --no-startup-id wicd-gtk -t
exec --no-startup-id /home/taoqi/.yong/yong/yong
exec --no-startup-id xbindkeys
exec --no-startup-id xbindkeys -fg $HOME/.xbindkeys.scm
# exec --no-startup-id udev-notify
# exec --no-startup-id myblueman
# exec --no-startup-id devmon
# exec --no-startup-id conky -c /home/monk/.weather/conkyrc
exec --no-startup-id basket -h
exec --no-startup-id diodon
exec --no-startup-id feh --bg-scale $HOME/.wallpaper
# exec --no-startup-id /home/monk/bin/dhdog
# exec --no-startup-id /home/monk/.screenlayout/AUTO.sh
exec --no-startup-id compton -cGb
# exec --no-startup-id sudo echo 10 > /sys/class/backlight/acpi_video0/brightness
exec --no-startup-id dropboxd
exec --no-startup-id ~/.i3/startup-ubuntu.sh

for_window [class="Wine"] floating enable;border none
for_window [class="Wine" instance="Thunder5.exe" title="迅雷5"] floating disable
for_window [class="Grun"] floating enable;border pixel 1
for_window [class="Quicksynergy"] floating enable;border normal
for_window [class="Stickynotes.py"] floating enable;border normal
for_window [class="Arandr"] floating enable;border normal
for_window [class="Main"] floating enable;border normal
for_window [class="Gscreenshot.py"] floating enable;border normal
for_window [class="Linux_text_input_gui.py"] floating enable;border normal
for_window [class="Gnome-mines"] floating enable;border normal
for_window [class="Gcolor2"] floating enable;border normal
for_window [class="Deepin-music-player"] floating enable
for_window [class="Dmp"] floating enable
for_window [title="PlayOnLinux"] floating enable;border normal
for_window [class="Demo.py"] floating enable;border normal
# for_window [class="Dolphin"] floating enable;border normal
for_window [class="File-roller"] floating enable;border normal
for_window [class="Cairo-clock"] floating enable;border normal
for_window [class="Yong-config"] floating enable;border normal
for_window [class="Gtk-recordMyDesktop"] floating enable;border normal
for_window [class="Kazam"] floating enable;border normal
for_window [class="Dropbox"] floating enable;border normal
for_window [class="Qsynergy"] floating enable;border normal

assign [class="Eclipse"] "1:code"
assign [class="Java" title="Eclipse"] "1:code"
# Chrome调试工具
# assign [class="Chromium" title="^未命名$"] 9

# exec --no-startup-id i3-msg "workspace 0:web"
